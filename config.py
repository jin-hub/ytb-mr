# -*- coding: utf-8 -*-
"""
集中配置。一般你不用改这里，日常都在 Google 表格里操作。
只有想调整报警灵敏度、推送间隔时才动这个文件。
"""

# ===== 时区 =====
TIMEZONE = "Asia/Seoul"   # 韩国时间 KST (UTC+9)，图表/表格/时间都用它

# ===== 采集 =====
FETCH_INTERVAL_MIN = 5    # 名义采集间隔（分钟）。GitHub Actions 实际可能略有延迟，已用速率算法消除影响。

# ===== 数据缺失判定 =====
GAP_THRESHOLD_MIN = 12    # 相邻两点间隔超过这么多分钟，视为数据缺失，缺失段不参与速率/异常判断

# ===== 定时推送趋势图的时间点（从每场“本轮开始”算起，单位：小时）=====
# 到达每个点时，推一次“该跨度”的趋势图（点赞图+播放图+数据表图）
PUSH_MILESTONES_H = [0.5, 1, 3, 6, 24, 48, 72]
# 48h 只画第 2 天数据（24~48h），72h 只画第 3 天数据（48~72h）
WINDOWED_MILESTONES = {48: (24, 48), 72: (48, 72)}

# ===== 异常检测（报警）参数 =====
# 不需要你设固定阈值，全部是“相对自己基线的偏离倍数”，已用你的真实台阶数据校准。
BASELINE_WINDOW = 6       # 滚动基线窗口（取最近 N 个速率点做基线）
MIN_POINTS_TO_JUDGE = 4   # 冷启动：速率点不足这么多就先不判断
K_DEVIATION = 4.0         # 偏离基线多少倍 MAD 算可疑（A条件）。越大越不灵敏。
B_RATIO_MIN = 2.0         # 脱离全组：自己速率/全组中位速率 的最低倍数（B条件）
B_RATIO_MULT = 3.0        # 且要超过自己平时相对比例的倍数
C_JUMP_MULT = 2.5         # 速率跳变：当前速率 > 基线中位的多少倍（C条件，权重最高）

# 条件权重：C > A = B（按你的要求）
W_A, W_B, W_C = 2, 2, 3
# 触发报警的规则：C命中即触发；或 A、B 中凑满两条（即 A+B）也触发
# （具体逻辑在 detector.py 里实现）

# ===== 异常报警附带的局部放大图：异常点前后各取多少小时 =====
ALERT_CONTEXT_HOURS = 2   # 异常前后各 2 小时（你要的 1~2 小时，取 2）

# ===== 报警去抖：同一(场,成员,指标)在这么多分钟内只报一次 =====
ALERT_DEDUP_MIN = 60

# ===== 监控自动停止 =====
AUTO_STOP_HOURS = 72      # 一轮监控满 72 小时后自动停（你也可在表格手动勾停止）

# ===== 指标 =====
METRICS = ["views", "likes"]   # 播放量、点赞量
METRIC_CN = {"views": "播放量", "likes": "点赞量"}
