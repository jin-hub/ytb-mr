# -*- coding: utf-8 -*-
"""
集中配置。一般你不用改这里，日常都在 Google 表格里操作。
只有想调整报警灵敏度、推送间隔时才动这个文件。
"""

# ===== 时区 =====
TIMEZONE = "Asia/Seoul"   # 韩国时间 KST (UTC+9)，图表/表格/时间都用它

# ===== 采集 =====
FETCH_INTERVAL_MIN = 5    # 名义采集间隔（分钟）。GitHub Actions 实际可能略有延迟，已用速率算法消除影响。

# ===== 数据缺失判定 =====
GAP_THRESHOLD_MIN = 12    # 相邻两点间隔超过这么多分钟，视为数据缺失，缺失段不参与速率/异常判断

# ===== 定时推送趋势图的时间点（从每场“本轮开始”T 算起，单位：分钟）=====
# 每个里程碑：到点时推 2 张趋势图（조회수 + 좋아요），范围见下表。
# 规律：前90分钟从 T 累积；之后为“固定90分钟滑动窗口”，每张往后挪。
# 格式：里程碑分钟 -> (窗口起点分钟, 窗口终点分钟)
PUSH_MILESTONES = [
    (30,    (0,    30)),     # T+30:  T ~ T+30
    (60,    (0,    60)),     # T+60:  T ~ T+60
    (90,    (0,    90)),     # T+90:  T ~ T+90
    (120,   (30,   120)),    # T+120: T+30 ~ T+120
    (180,   (90,   180)),    # T+180: T+90 ~ T+180
    (240,   (150,  240)),    # T+240: T+150 ~ T+240
    (300,   (210,  300)),    # T+300: T+210 ~ T+300
    (360,   (270,  360)),    # T+360(6h): T+270 ~ T+360
    (1440,  (0,    1440)),   # 24h:   0 ~ 24h（全程）
    (2880,  (1440, 2880)),   # 48h:   第2天（24~48h）
    (4320,  (2880, 4320)),   # 72h:   第3天（48~72h）
]

# 数据表（排名快照）只在这些里程碑（分钟）推：30min/1h/6h/24h/48h/72h
TABLE_MILESTONES = [30, 60, 360, 1440, 2880, 4320]

# ===== 监控自动停止 =====
AUTO_STOP_HOURS = 72      # 一轮监控满 72 小时后自动停（你也可在表格手动勾停止）

# ===== 指标 =====
METRICS = ["views", "likes"]   # 播放量、点赞量
METRIC_CN = {"views": "조회수", "likes": "좋아요"}
METRIC_EN = {"views": "views", "likes": "likes"}  # 文件名用，纯英文
